{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "bench": {
      "dependsOn": ["^build"]
    },
    "build:watch": {},
    "check:exports": {
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false,
      "outputs": ["packages/*/build"]
    }
  }
}
