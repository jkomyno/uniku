{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "bench": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "__tests__/bench/**"],
      "outputs": ["bench-results.json"]
    },
    "bench:compat": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "__tests__/bench/**"],
      "outputs": ["bench-results.json"]
    },
    "bench:summary": {
      "dependsOn": ["bench:compat"],
      "inputs": ["bench-results.json", "scripts/bench-summary.ts"],
      "outputs": []
    },
    "bundle:summary": {
      "dependsOn": ["^build"]
    },
    "test:e2e": {
      "dependsOn": ["^build"]
    },
    "test:e2e:cloudflare": {
      "dependsOn": ["^build"]
    },
    "build:watch": {},
    "clean": {
      "cache": false,
      "outputs": ["packages/*/build"]
    }
  }
}
