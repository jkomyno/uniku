{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "bench": {
      "dependsOn": ["^build"]
    },
    "build:watch": {},
    "clean": {
      "cache": false,
      "outputs": ["packages/*/build"]
    }
  }
}
