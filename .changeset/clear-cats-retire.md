---
"uniku": minor
---

Publish the final pre-v1 migration release with the `secs`, `seq`, and Nanoid object-form `size` aliases explicitly deprecated. Use `msecs`, `counter`, and `length` instead; the aliases will be removed in `1.0.0-rc.1`.
