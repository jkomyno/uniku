---
"@uniku/cli": patch
---

Harden CLI installers by creating missing install directories, keeping shell install POSIX-compatible, paginating release lookup, and applying chmod with sudo when needed.
