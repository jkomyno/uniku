---
"@uniku/cli": minor
---

Publish a standalone Linux ARM64 CLI binary alongside the existing Linux x64/amd64 and macOS binaries. The installer now detects both `aarch64` and `arm64` Linux hosts and downloads the matching release asset.
