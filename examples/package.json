{
  "name": "@uniku/examples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "pnpm run typecheck && pnpm run test",
    "example:drizzle": "bun test --conditions=@jkomyno/source ./drizzle-ulid.ts",
    "example:effect": "bun test --conditions=@jkomyno/source ./effect-typeid.ts",
    "example:hono": "bun test --conditions=@jkomyno/source ./hono-nanoid.ts",
    "test": "pnpm run example:hono && pnpm run example:drizzle && pnpm run example:effect",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.5.4",
    "drizzle-orm": "1.0.0-rc.4",
    "effect": "4.0.0-beta.93",
    "hono": "catalog:",
    "uniku": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "catalog:"
  }
}
